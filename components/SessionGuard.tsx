"use client";

// Keeps the shared layout import stable.
// Browser/device security is still enforced by the API routes and page-level calls.
export default function SessionGuard() {
  return null;
}
