"use client";
import Link from "next/link";
import { useUI } from "@/lib/ui-context";
export default function PricingSuccess(){const{language}=useUI();const ro=language==="ro";return <main className="platform-page"><section className="platform-hero" style={{textAlign:"center"}}><span className="status-badge">✓ {ro?"Plată confirmată":"Payment confirmed"}</span><h1 className="platform-title" style={{marginInline:"auto"}}>{ro?"Planul este în curs de sincronizare.":"Your plan is being synchronized."}</h1><p className="platform-subtitle" style={{marginInline:"auto"}}>{ro?"Webhook-ul Stripe actualizează automat creditele sau abonamentul. În mod normal durează doar câteva secunde.":"The Stripe webhook automatically updates credits or your subscription. This normally takes only a few seconds."}</p><div style={{display:"flex",justifyContent:"center",gap:10,marginTop:24,flexWrap:"wrap"}}><Link className="platform-button" href="/dashboard">{ro?"Mergi la panou":"Go to dashboard"}</Link><Link className="platform-button secondary" href="/pricing">{ro?"Vezi planurile":"View plans"}</Link></div></section></main>}
